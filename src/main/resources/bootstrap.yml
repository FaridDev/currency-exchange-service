spring:
  application:
    name: currency-exchange-service
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
server:
  port: ${PORT:8000}

eureka:
  client:
    service-url: http://localhost:8761