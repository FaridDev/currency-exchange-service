spring:
  application:
    name: currency-exchange-service
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true
  sleuth:
    sampler:
      percentage: 1.0
server:
  port: ${PORT:8000}

eureka:
  client:
    service-url: http://localhost:8761